{
  "name": "每日签到任务",
  "description": "每天 9:00 自动执行 tasks.yaml 中启用的签到任务（New API / ViewTurbo）",
  "workflow": {
    "triggers": [
      {
        "cron": {
          "cron": "0 9 * * *"
        }
      }
    ],
    "actions": [],
    "prompts": [
      "你是每日任务执行器。在本仓库根目录依次执行：\n\n1. npm ci\n2. npx playwright install chromium --with-deps\n3. npm run tasks\n\n要求：\n- 使用已配置的 NEWAPI_ACCOUNTS（Cookie 签到）和 VIEWTURBO_*（浏览器签到）\n- 按 tasks.yaml 中 enabled 的任务汇报：签到成功 / 今日已签到 / 失败原因\n- 若 npm run tasks 退出码非 0，说明失败任务与日志路径 logs/\n- 不要修改 tasks.yaml 或业务代码，除非任务明确失败且需要修复"
    ],
    "model": "",
    "agentOptions": {
      "skipInstall": false
    },
    "memoryEnabled": true,
    "gitConfig": {
      "repo": "qianjunjian/day-task",
      "branch": "main"
    }
  }
}
